temp_sensor(temp) matches {temperature:temp};

Main = CheckTemp<20>;
CheckTemp<temp> = if (temp>=15 && temp <= 35) {let temp; temp_sensor(temp) (empty \/ CheckTemp<temp>)} else none;  

// Main = Check<10,40>;
// Check<min,max> = {let temp; temp_sensor(temp) with (temp>=min && temp <= max)}*;
